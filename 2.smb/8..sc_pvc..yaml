apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-azsmb
  namespace: code-server
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "azsmb"
  resources:
    requests:
      storage: 10Gi