apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-azsmb
  namespace: code-server
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "az-smb"
  resources:
    requests:
      storage: 10Gi